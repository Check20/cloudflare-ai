# Cloudflare AI Integration
Efficient Python implementation for Cloudflare Workers AI. Built for secure, high-performance inference using Google's Gemma models.
## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Export your Cloudflare credentials as environment variables.
